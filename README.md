# SborrBot

<p align="center">
  <br>
  <a href="https://t.me/SborrrrBot_bot?startgroup=true">
    <img src="https://img.shields.io/badge/%F0%9F%8D%86%F0%9F%92%A6_FICCALO_NEL_GRUPPO_ADESSO-PREMI_QUI_BRUTTO_PORCO-ff1493?style=for-the-badge&logo=telegram&logoColor=white&labelColor=8b0000&link=https://t.me/SborrrrBot_bot?startgroup=true" alt="FICCALO NEL GRUPPO" height="70">
  </a>
  <br><br>
  <a href="https://t.me/SborrrrBot_bot?startgroup=true">
    <img src="https://img.shields.io/badge/CHE_ASPETTI%3F-HAI_PAURA%3F_CAGASOTTO-ffd700?style=flat-square&labelColor=000000" alt="Che aspetti?" height="25">
  </a>
  <br>
  <sub><i>Clicca il bottone qua sopra, non fare il timido del cazzo. Il tuo gruppo ha bisogno di essere devastato.</i></sub>
  <br><br>
</p>

**Il bot Telegram piu sborrante che esista.** Ispirato al leggendario SpacoBot, ma piu cattivo, piu scurrile, piu bastardo e con un pannello admin che manco la NASA. Oltre **3000 frasi** precaricate nel database, tutte scritte con l'odio e la poesia di chi ha visto cose che voi umani non potete immaginare.

Insulta i tuoi amici, mandali affanculo con stile, bestemmia come Germano Mosconi, fai piovere tette e culi nelle chat di gruppo, fai l'esorcismo al collega stronzo, mettilo all'asta su eBay e fagli la televendita come Mastrota. Tutto su Cloudflare Workers, perche anche le porcherie meritano un'infrastruttura enterprise da paura.

**Landing page**: [sborrbot.pages.dev](https://sborrbot.pages.dev)

---

## Che cazzo fa questo bot?

SborrBot e un bot di intrattenimento per gruppi Telegram che risponde a keyword naturali (niente slash del cazzo per i comandi principali). Scrivi "insulta Mario" e il bot gli fa il culo a strisce. Scrivi "bestemmia" e ti parte un porco dio che Germano Mosconi si gira nella tomba. Scrivi IN CAPSLOCK E IL BOT TI RISPONDE PERCHE SEI UN COGLIONE CHE URLA.

Ha **50+ comandi**, reagisce automaticamente con le emoji ai messaggi, ha un sistema di messaggi programmati per bombardare i gruppi, un meteo che ti dice il tempo e poi ti insulta, e un backoffice admin con dashboard, analytics e gestione contenuti che manco Pornhub Premium.

---

## Comandi Testuali (scrivi e godi)

### Insulti e Devastazione Personale

| Comando | Che cazzo fa |
|---------|-------------|
| `insulta [nome]` | Genera un insulto personalizzato per la vittima designata. 50+ varianti, mai lo stesso due volte (o quasi) |
| `insulta combo [nome]` | Triplo insulto devastante in sequenza. FATALITY. BRUTALITY. Il nome viene demolito tre volte di fila senza pieta |
| `minaccia [nome]` | Minaccia di morte (scherzosa... forse) il malcapitato. Roba che i Carabinieri bussano alla porta |
| `necrologio [nome]` | Elogio funebre del designato. Riposa in pace, pezzo di merda |
| `processo [nome]` | Processo penale completo con sentenza. Il giudice e il bot, e il bot non perdona |
| `cv [nome]` | Curriculum vitae devastante. Se lo mandi a un colloquio con sto CV lo assumono al circo |
| `complimento [nome]` | Un complimento... a modo nostro. Tipo "sei la prova che dio ha il senso dell'umorismo" |

### Referti Medici e Scientifici

| Comando | Che cazzo fa |
|---------|-------------|
| `autopsia [nome]` | Referto autoptico dettagliato. Causa della morte: essere un coglione |
| `diagnosi [nome]` | Diagnosi medica impietosa. Il dottore consiglia l'eutanasia |
| `esorcismo [nome]` | Rituale di esorcismo completo. Il demone se ne va perche pure lui si e rotto il cazzo |

### Profili e Presentazioni

| Comando | Che cazzo fa |
|---------|-------------|
| `chi e [nome]` | Presentazione biografica del soggetto. Wikipedia degli stronzi |
| `giudizio [nome]` | Giudizio insindacabile e definitivo. La corte ha deciso: sei una merda |
| `tinder [nome]` | Profilo Tinder della vittima. Bio, interessi, red flags. Swipe left garantito |

### Televendite e Commercio

| Comando | Che cazzo fa |
|---------|-------------|
| `televendita [nome]` | Il nome viene venduto in televendita stile Mastrota. "MA NON E FINITA QUI!" Prezzo stracciato perche vale poco |
| `asta [nome]` | Asta eBay del soggetto. Descrizione prodotto, difetti, spedizione, feedback negativi. Base d'asta: 1 centesimo |

### Saggezza Popolare e Filosofia Spicciola

| Comando | Che cazzo fa |
|---------|-------------|
| `bestemmia` | Una bella bestemmia fresca fresca. Germano Mosconi approved |
| `come diceva mio nonno` | Saggezza popolare rivisitata in chiave scurrile. Il nonno ne sapeva di piu di Confucio |
| `frase celebre` / `citazione` | Perla di saggezza. Frasi celebri... rivisitate |
| `profezia` | Profezia personalizzata per chi scrive. Nostradamus era un dilettante |
| `consiglio` | Consiglio (non) utile. Se lo segui, peggio per te |
| `motivazione` | Frase motivazionale... a modo nostro. Per iniziare la giornata col piede sbagliato |
| `fact check` | Fact check del cazzo. Verifica di fatti inventati con fonti inesistenti |
| `ricetta` | Ricetta dello chef. Ingredienti: bestemmie e disperazione |

### Saluti e Convenevoli

| Comando | Che cazzo fa |
|---------|-------------|
| `buongiorno` / `buonanotte` / `ciao` | Saluti personalizzati dallo Sborratore. Neanche tua madre ti saluta cosi |
| `grazie` | Ringraziamento con la delicatezza che ci contraddistingue |
| `auguri` / `buon compleanno` | Auguri personalizzati. Un anno piu vicino alla morte, congratulazioni |

### Oroscopo e Meteo

| Comando | Che cazzo fa |
|---------|-------------|
| `oroscopo [segno]` | Oroscopo osceno per tutti i 12 segni. Se non specifichi il segno ne becca uno a caso e ti insulta lo stesso |
| `meteo [citta]` | Meteo REALE via OpenWeatherMap + commento da stronzi. Ti dice che piove e poi ti prende per il culo. 8 categorie meteo con 8 template ciascuna |

### Comandi di Gruppo (coinvolgono membri random)

Questi comandi pescano membri random dal gruppo per creare scenari assurdi. Piu persone scrivono nel gruppo, piu vittime ci sono da pescare.

| Comando | Che cazzo fa |
|---------|-------------|
| `notizia` | FLASH NEWS che coinvolge un membro random del gruppo. Breaking news: Luca ha fatto una cagata pazzesca |
| `segreto` | Rivela un segreto tra due membri random. "Ho scoperto che Marco e Luigi..." |
| `bollettino` | Bollettino ufficiale del gruppo. Comunicati stampa surreali |
| `complotto` | Teoria del complotto che coinvolge due membri. I poteri forti non vogliono che tu sappia |
| `intercettazione` | Intercettazione telefonica tra due membri random. Roba da Guardia di Finanza |
| `eredita` / `testamento` | Testamento aperto: un membro lascia le sue cose a un altro. Spoiler: lascia solo debiti e vergogna |

---

## Auto-trigger (il bot risponde ai cazzi tuoi)

SborrBot non aspetta che lo chiami. Lui ASCOLTA. Lui GIUDICA. Lui RISPONDE quando gli pare.

### Squadre di Calcio

Nomina una squadra e il bot ti asfalta.

| Keyword | Che succede |
|---------|-------------|
| `juve`, `juventus`, `gobbi`, `bianconeri` | Insulti anti-Juve + reazione 💩 |
| `roma`, `romanista`, `giallorossi`, `trigoria` | Insulti anti-Roma + reazione 🤡 |
| `lazio`, `laziale`, `biancocelesti` | Insulti anti-Lazio + reazione 🤡 |
| `milan`, `milanista`, `rossoneri` | Insulti anti-Milan + reazione 🤡 |

### Lamenti e Piagnistei

| Keyword | Che succede |
|---------|-------------|
| `ho fame`, `sono stanco`, `che noia`, `sono triste`, `che palle` | Ti prende per il culo perche sei un piagnone + reazione 🤣 |

### Ego Smisurato

| Keyword | Che succede |
|---------|-------------|
| `sono il migliore`, `sono forte`, `sono un genio` | Ridimensionamento brutale. Riportato sulla terra col paracadute rotto |

### Stereotipi Regionali

| Keyword | Che succede |
|---------|-------------|
| `napoli`, `pizza`, `maradona`, `pulcinella`, `camorra`, `vesuvio`, `sfogliatella`, `baba`, `ragu`, `fuorigrotta` | Stereotipi napoletani a raffica + reazione 🍕 |

### Palestra e Fitness

| Keyword | Che succede |
|---------|-------------|
| `palestra`, `dieta`, `fitness`, `crossfit`, `proteine` | Prese per il culo fitness. "Ah fai crossfit? Cioe paghi per farti male?" |

### Ex e Relazioni

| Keyword | Che succede |
|---------|-------------|
| `la mia ex`, `il mio ex`, `ex ragazza`, `ex ragazzo` | Commenti sulla tua vita sentimentale disastrosa + reazione 💔 |

### Terapia e Salute Mentale

| Keyword | Che succede |
|---------|-------------|
| `terapia`, `psicologo`, `seduta` | "Ci credo che vai dallo psicologo, ti serve" + reazione 🧠 |

### CAPSLOCK (IL BOT SENTE CHE URLI)

Se scrivi un messaggio di 10+ caratteri con il 70%+ in MAIUSCOLO, il bot ti risponde perche stai urlando come un coglione. Stai calmo, cazzo.

---

## Reazioni Emoji Automatiche

Il bot reagisce automaticamente ai messaggi con emoji, come un amico stronzo che mette le faccine sotto i tuoi post:

| Trigger | Reazione |
|---------|----------|
| Keyword Juventus | 💩 |
| Lamenti e piagnistei | 🤣 |
| Bestemmie | 😱 |
| Roba napoletana | 🍕 |
| Calcio generico | 🤡 |
| Ex e relazioni | 💔 |
| Terapia | 🧠 |

---

## Comandi Foto (NSFW)

| Comando | Che fa |
|---------|--------|
| `fica` | Invia una foto di fica random |
| `culo` | Culi. Tanti culi. |
| `tette` | Tette. Che domande. |
| `degrado` | Il peggio del peggio. Roba che ti fa rivalutare le tue scelte di vita |

> I contenuti NSFW li devi caricare tu dal backoffice. Il bot non genera porno da solo (ancora).

---

## Comandi Audio

| Comando | Chi parla |
|---------|-----------|
| `germano mosconi` | Il mitico Germano Mosconi. PORCO DIO IN DIRETTA. |
| `christian de sica` | Le perle di Christian De Sica. "E allora? VACANZE DI NATALE!" |
| `homer simpson` | D'oh! Le citazioni di Homer |
| `i soliti idioti` | Audio de I Soliti Idioti. "ESAGERATO!" |
| `richard benson` | IL GRANDE RICHARD BENSON. ROMPETE TUTTO! |
| `effetti sonori` | Effetti sonori vari per ogni occasione |

> Il sistema di caching salva il `telegram_file_id` dopo il primo invio. Le volte successive il bot manda il file istantaneamente senza ri-scaricarlo da R2. Piu veloce di te a venire.

---

## Sticker

| Comando | Sticker |
|---------|---------|
| `apple` | Steve Jobs che ti giudica |
| `banana` / `minion` | Minion del cazzo |
| `non ci sono` | Sticker di disapprovazione totale |

---

## Controllo

| Comando | Effetto |
|---------|---------|
| `zitto sborrbot` | Metti a cuccia il bot nel gruppo. Sta zitto ma non se ne va, come il tuo coinquilino |
| `sveglia sborrbot` | Risveglia la bestia. Preparate l'ano |

---

## Messaggi Programmati (Broadcast)

SborrBot ha un sistema di messaggi programmati che ti permette di bombardare i gruppi a orari prestabiliti. Come una sveglia, ma peggio.

| Tipo | Come funziona |
|------|---------------|
| **Una tantum** | Mandi un messaggio a una data/ora specifica. Poi basta, come la tua ex |
| **Giornaliero** | Ogni giorno alla stessa ora. Il buongiornissimo automatico che nessuno ha chiesto |
| **Settimanale** | Un giorno specifico della settimana. Per ricordarti che il lunedi fa schifo |

Puoi mandare a **tutti i gruppi attivi** o a un **gruppo specifico**. Configurabile dal backoffice nella sezione Scheduler.

Il cron gira **ogni minuto** per controllare se ci sono messaggi da sparare. Precisione svizzera al servizio della cazzata.

---

## Slash Commands

| Comando | Descrizione |
|---------|-------------|
| `/start` | Messaggio di benvenuto. Il bot si presenta come il pezzo di merda che e |
| `/help` | Lista completa dei comandi. Questa lista, ma su Telegram |
| `/info` | Info sul bot e crediti |
| `/testo` | Lista categorie testo |
| `/foto` | Lista categorie foto |
| `/audio` | Lista categorie audio |
| `/sticker` | Lista categorie sticker |
| `/privacy` | Informativa privacy (GDPR Art. 13). Si, pure il bot delle bestemmie rispetta il GDPR |
| `/mydata` | Visualizza i tuoi dati (GDPR Art. 15). Scopri quante volte hai scritto "fica" |
| `/deleteme` | Cancella tutti i tuoi dati (GDPR Art. 17). Diritto all'oblio, come se non fossi mai esistito |

---

## Architettura

```
                        +------------------+
                        |  Telegram API    |
                        +--------+---------+
                                 | Webhook POST
                                 v
+------------------+     +-------+--------+     +------------------+
|   Astro SSR      |     | CF Worker      |     | OpenWeatherMap   |
|   Backoffice     |     | (Bot Handler)  |     | API              |
|   + Tailwind     |     +--+-------+-----+     +------------------+
+--------+---------+        |       |
         |                  v       v
         |            +-----+--+ +--+------+
         +----------->|   D1   | |   R2    |
                      |Database| | Storage |
                      +--------+ +---------+

Cron Triggers:
  * * * * *     --> Worker --> Check messaggi programmati
  0 3 * * *     --> Worker --> Purge logs > 90 giorni
```

### Stack Tecnologico

| Componente | Tecnologia |
|-----------|------------|
| **Bot** | Cloudflare Worker (TypeScript) |
| **Database** | Cloudflare D1 (SQLite at the edge) |
| **Media Storage** | Cloudflare R2 (S3-compatible, zero egress fees) |
| **Backoffice** | Astro 5 + Tailwind CSS su Cloudflare Pages (auto-deploy da Git) |
| **Landing Page** | Astro SSR su Cloudflare Pages |
| **Auth** | Session cookie + D1 + Cloudflare Turnstile CAPTCHA |
| **Meteo** | OpenWeatherMap API (real-time) |
| **Cron** | Cloudflare Cron Triggers (broadcast ogni minuto + purge log giornaliero) |
| **Contenuti** | 3000+ frasi pre-caricate in 40+ categorie |

### Struttura del Progetto

```
SborrBot/
├── worker/                 # Il cervello del bot (Cloudflare Worker)
│   ├── src/
│   │   ├── index.ts        # Entry point: webhook + cron scheduler (broadcast + purge)
│   │   ├── bot.ts          # Dispatcher principale + emoji reactions
│   │   ├── types.ts        # Env interface
│   │   ├── commands/
│   │   │   ├── slash.ts    # /start, /help, /privacy, /mydata, /deleteme, /info, /testo, /foto, /audio, /sticker
│   │   │   ├── text.ts     # 30+ comandi testuali + auto-trigger + capslock detection
│   │   │   ├── media.ts    # Audio e foto (R2 -> Telegram con caching file_id)
│   │   │   ├── sticker.ts  # Sticker per file_id
│   │   │   └── control.ts  # zitto/sveglia
│   │   ├── middleware/
│   │   │   ├── group-check.ts  # Verifica se il bot e attivo nel gruppo + auto-tracking
│   │   │   └── logger.ts       # Logga ogni comando su D1 (chi, dove, quando, cosa)
│   │   └── services/
│   │       ├── db.ts        # Query D1 (random content, GDPR, group users, scheduling)
│   │       ├── storage.ts   # Lettura R2 (getMediaFile, getMediaAsArrayBuffer)
│   │       ├── telegram.ts  # Client API Telegram (text, photo, audio, sticker, reactions, webhook)
│   │       └── weather.ts   # Client OpenWeatherMap (8 categorie meteo x 8 template)
│   └── wrangler.toml
│
├── backoffice/             # Pannello admin + landing page (Astro + Tailwind)
│   ├── src/
│   │   ├── layouts/Layout.astro    # Layout dark con sidebar responsive
│   │   ├── middleware.ts           # Auth guard (session + D1 + path normalization)
│   │   ├── pages/
│   │   │   ├── index.astro         # Landing page pubblica
│   │   │   ├── login.astro         # Login (Turnstile CAPTCHA + credenziali)
│   │   │   ├── dashboard.astro     # Statistiche, trend, grafici, top utenti/gruppi
│   │   │   ├── content/            # CRUD testi, audio, foto, sticker
│   │   │   ├── categories.astro    # Gestione categorie (CRUD completo)
│   │   │   ├── groups.astro        # Gestione gruppi (pausa/ban)
│   │   │   ├── logs.astro          # Log comandi searchable e filtrabile
│   │   │   ├── settings.astro      # Configurazione bot
│   │   │   └── scheduler.astro     # Messaggi programmati (once/daily/weekly)
│   │   ├── pages/api/              # REST API complete
│   │   │   ├── auth/               # Login + logout
│   │   │   ├── content/            # CRUD testi, media upload (50MB max), sticker, bulk import (500 max)
│   │   │   ├── categories/         # CRUD categorie
│   │   │   ├── groups/             # Gestione gruppi
│   │   │   ├── scheduler/          # CRUD messaggi programmati
│   │   │   ├── config/             # Configurazione globale
│   │   │   └── search/             # Ricerca globale testi/comandi
│   │   └── lib/
│   │       ├── auth.ts             # Constant-time password comparison (HMAC), session CRUD, token 32 byte
│   │       └── db.ts               # Query D1 per backoffice
│   ├── public/
│   │   ├── _headers                # Security headers (CSP, HSTS, X-Frame-Options, etc.)
│   │   ├── .well-known/security.txt
│   │   ├── robots.txt
│   │   └── sitemap.xml
│   └── wrangler.toml
│
├── migrations/             # Schema D1 (16 file, ~4000 righe di contenuti)
│   ├── 0001_initial_schema.sql      # Schema base: 9 tabelle
│   ├── 0002_seed_data.sql           # Categorie + contenuti iniziali
│   ├── 0003_bulk_content.sql        # 100+ insulti, minacce
│   ├── 0003_anti_juve.sql           # Contenuti anti-Juventus dedicati
│   ├── 0004_extra_dirty_content.sql # Contenuti NSFW/estremi
│   ├── 0005_new_features.sql        # Oroscopo, frasi celebri (300+ items)
│   ├── 0006_media_uploads.sql       # Metadata media R2
│   ├── 0007_new_features_2.sql      # Nuove risposte (350+ items)
│   ├── 0008_new_features_3.sql      # CV, complimenti, diagnosi (300+ items)
│   ├── 0009_extreme_features.sql    # Contenuti estremi (800+ items)
│   ├── 0010_napoletano.sql          # Stereotipi napoletani (150+ items)
│   ├── 0011_new_triggers.sql        # Lamenti, palestra, ego, ex, terapia (250+ items)
│   ├── 0012_tinder_profezia.sql     # Profili Tinder, profezie (300+ items)
│   ├── 0013_bollettino_capslock.sql # Bollettini, risposte capslock (140+ items)
│   ├── 0014_scheduled_messages.sql  # Tabella messaggi programmati
│   └── 0015_televendita_asta_esorcismo.sql  # Televendita, aste, esorcismi (1000+ items)
│
├── shared/                 # Tipi TypeScript condivisi tra worker e backoffice
│   └── types.ts
│
└── package.json            # Root monorepo (npm workspaces)
```

---

## Setup

### Prerequisiti

- Node.js 18+
- Un account Cloudflare (gratis, come il tuo valore)
- Un bot Telegram (crealo su [@BotFather](https://t.me/BotFather))
- `wrangler` CLI installato (`npm install -g wrangler`)
- Una chiave API OpenWeatherMap (gratis, per il meteo)

### 1. Clona e installa

```bash
git clone https://github.com/amargiovanni/SborrBot.git
cd SborrBot
npm install
cd worker && npm install && cd ..
cd backoffice && npm install && cd ..
```

### 2. Crea le risorse Cloudflare

```bash
wrangler login

# Crea il database D1
wrangler d1 create sborrbot-db
# -> Copia il database_id e mettilo in worker/wrangler.toml E backoffice/wrangler.toml

# Crea il bucket R2
wrangler r2 bucket create sborrbot-media
```

### 3. Aggiorna i wrangler.toml

Prendi il `database_id` dall'output del comando precedente e sostituiscilo in:
- `worker/wrangler.toml`
- `backoffice/wrangler.toml`

### 4. Esegui le migrazioni

```bash
cd worker

# Locale (per sviluppo)
for f in ../migrations/*.sql; do
  npx wrangler d1 execute sborrbot-db --local --file="$f"
done

# Remoto (per produzione)
for f in ../migrations/*.sql; do
  npx wrangler d1 execute sborrbot-db --remote --file="$f"
done

cd ..
```

> **Nota sui media:** Le migrazioni creano schema, 3000+ testi di esempio e tutte le categorie. I record media puntano a file R2 che **non sono inclusi nel repo** (vanno caricati tramite il backoffice). I comandi testuali funzionano subito dopo le migrazioni — hai gia abbastanza materiale per far piangere chiunque.

### 5. Configura i secrets

```bash
# Worker
cd worker
wrangler secret put BOT_TOKEN              # Token da BotFather
wrangler secret put BOT_SECRET             # Stringa casuale per validare webhook
wrangler secret put OPENWEATHERMAP_API_KEY # API key da openweathermap.org (per il meteo)
cd ..

# Backoffice (via Cloudflare Dashboard -> Pages -> Settings -> Environment Variables)
# ADMIN_USERNAME       — username per il login
# ADMIN_PASSWORD       — password per il login
# TURNSTILE_SECRET_KEY — secret key da Cloudflare Turnstile
# TURNSTILE_SITE_KEY   — site key da Cloudflare Turnstile
```

### 6. Deploy

```bash
# Deploy il worker
cd worker
npx wrangler deploy
# -> URL: https://sborrbot-worker.tuousername.workers.dev

# Il backoffice fa auto-deploy da Git (Cloudflare Pages con Git provider)
# Ogni push su main deploya automaticamente. CI/CD delle bestemmie.
```

### 7. Registra il webhook

```bash
curl -H "Authorization: Bearer TUO_BOT_SECRET" \
  https://sborrbot-worker.tuousername.workers.dev/register
```

Se vedi `{"ok":true}` sei a posto. Il bot e vivo e pronto a sborrare.

---

## Backoffice

Il pannello admin e una figata. Dark theme, sidebar, tutto responsive. Piu bello del sito della tua azienda, garantito.

### Come accedere

1. Vai su `https://sborrbot.pages.dev/login`
2. Inserisci le credenziali configurate nelle environment variables
3. Supera il CAPTCHA Turnstile (si, pure per entrare nel pannello delle bestemmie serve il CAPTCHA)
4. Goditi il pannello

### Cosa puoi fare

| Sezione | Che ci fai |
|---------|------------|
| **Dashboard** | Statistiche complete: comandi eseguiti, gruppi attivi, comandi piu usati, trend 7/30 giorni, attivita oraria, top gruppi e top utenti. Grafici e numeri per sentirti un CEO della merda |
| **Testi** | Aggiungi/elimina frasi per ogni categoria. Usa `{name}` come placeholder per la vittima, `{name1}`/`{name2}` per comandi con due target. Supporta bulk import fino a 500 frasi alla volta |
| **Audio** | Carica file audio (mp3, ogg, wav, mp4) per ogni personaggio. Max 50MB per file. Il bot li scarica da R2 e li manda su Telegram |
| **Foto** | Carica foto (jpeg, png, gif, webp) per le categorie NSFW. Max 50MB |
| **Sticker** | Aggiungi sticker tramite `file_id` di Telegram |
| **Categorie** | CRUD completo delle categorie. Crea nuove categorie, rinomina, attiva/disattiva. Lo slug viene sanitizzato automaticamente |
| **Gruppi** | Vedi tutti i gruppi dove il bot e presente. Mettili in pausa o bannali. Statistiche per gruppo |
| **Log** | Cerca e filtra ogni singolo comando eseguito dal bot. Chi ha scritto cosa, dove e quando. Il Grande Fratello delle bestemmie |
| **Settings** | Configurazione globale del bot (key-value) |
| **Scheduler** | Programma messaggi broadcast: una tantum, giornalieri o settimanali. Scegli il gruppo target o manda a tutti |

### API REST del Backoffice

Il backoffice espone API REST complete per ogni operazione:

| Endpoint | Che fa |
|----------|--------|
| `POST /api/auth/login` | Login con verifica Turnstile |
| `POST /api/auth/logout` | Logout (distrugge sessione) |
| `POST /api/content/texts` | Aggiungi frase |
| `DELETE /api/content/texts?id=X` | Elimina frase |
| `POST /api/content/upload` | Upload media su R2 (50MB max) |
| `DELETE /api/content/media?id=X` | Elimina media (R2 + D1) |
| `POST /api/content/stickers` | Aggiungi sticker |
| `POST /api/content/bulk-import` | Import massivo (max 500 frasi) |
| `POST /api/categories` | Crea categoria |
| `PUT /api/categories` | Modifica categoria |
| `DELETE /api/categories?id=X` | Elimina categoria (solo se vuota) |
| `PUT /api/groups` | Modifica stato gruppo |
| `GET/POST/PUT/DELETE /api/scheduler` | CRUD messaggi programmati |
| `PUT /api/config` | Modifica configurazione globale |
| `GET /api/search?q=...` | Ricerca globale (min 2 caratteri) |

---

## Database

9 tabelle in D1 (SQLite at the edge). Il database delle bestemmie piu distribuito al mondo:

| Tabella | Che ci sta dentro |
|---------|-------------------|
| `categories` | 40+ categorie di contenuti. Tipo: text, audio, photo, sticker. Attivabili/disattivabili |
| `text_responses` | 3000+ frasi per le categorie testuali. Placeholder: `{name}`, `{name1}`, `{name2}`. Selezione random con `ORDER BY RANDOM() LIMIT 1` |
| `media` | Metadata dei file su R2 (audio, foto, sticker) + cache `telegram_file_id` per invio istantaneo |
| `groups` | Gruppi Telegram tracciati con stato: attivo / in pausa / bannato |
| `group_settings` | Impostazioni per gruppo (key-value) |
| `command_logs` | Log di ogni comando: chi, dove, quando, che ha chiesto, tipo risposta. Purge automatico 90 giorni |
| `bot_config` | Configurazione globale key-value |
| `sessions` | Sessioni login backoffice. Token 32 byte, scadenza 24h, cleanup automatico |
| `scheduled_messages` | Messaggi programmati: tipo (once/daily/weekly), orario, gruppo target, stato |

---

## Storage R2

```
sborrbot-media/
├── audio/
│   ├── germano-mosconi/     # PORCO DIO collection
│   ├── christian-de-sica/   # Vacanze di Natale collection
│   ├── homer-simpson/       # D'oh collection
│   ├── soliti-idioti/       # ESAGERATO collection
│   ├── richard-benson/      # ROMPETE TUTTO collection
│   ├── effetti-sonori/      # Per ogni occasione
│   └── audio-vari/          # Il resto
├── photos/
│   ├── fica/                # Autoesplicativo
│   ├── culo/                # Idem
│   ├── tette/               # Pure questo
│   └── degrado/             # Il fondo del barile
└── stickers/
    ├── apple/               # Steve Jobs giudicante
    ├── minion/              # Banana
    └── disapprovazione/     # No.
```

I file vengono caricati dal backoffice e serviti dal worker. La prima volta che il bot invia un file su Telegram, salva il `telegram_file_id` in D1 per non dover ri-uploadare ogni volta. Dalla seconda volta in poi e istantaneo. Tipo lo sborratore precoce, ma in senso buono.

MIME supportati:
- **Audio**: `audio/mpeg`, `audio/ogg`, `audio/wav`, `audio/mp4`
- **Immagini**: `image/jpeg`, `image/png`, `image/gif`, `image/webp`
- **Dimensione max**: 50MB per file

---

## Privacy e GDPR

SborrBot prende la privacy sul serio (no, davvero). E il bot delle bestemmie piu GDPR-compliant del pianeta.

### Dati raccolti

Il bot raccoglie il minimo indispensabile per funzionare:
- **Telegram user ID** (numerico, non il tuo nome vero)
- **Username** (quello pubblico su Telegram, che hai scelto tu)
- **Comando inviato** e timestamp (per i log)
- **ID del gruppo** in cui e stato usato

### Diritti dell'utente

| Comando | GDPR | Cosa fa |
|---------|------|---------|
| `/privacy` | Art. 13 | Mostra informativa completa sulla raccolta dati. Legalese tradotto in italiano comprensibile |
| `/mydata` | Art. 15 | Mostra i dati raccolti: totale comandi, prima e ultima interazione. Scopri quante bestemmie hai chiesto |
| `/deleteme` | Art. 17 | Cancella TUTTI i tuoi dati dal database. Diritto all'oblio. Come se non fossi mai stato qui |

### Retention automatica

Un cron trigger gira ogni giorno alle 03:00 UTC e cancella automaticamente tutti i log piu vecchi di **90 giorni** (Art. 5.1.e — limitazione della conservazione). Il bot dimentica le tue cazzate dopo 3 mesi. Piu di quanto faccia tua madre.

---

## Sicurezza

Per essere un bot che manda bestemmie, la sicurezza e presa maledettamente sul serio.

### Misure implementate

| Misura | Dettaglio |
|--------|-----------|
| **Auth backoffice** | Session cookie + D1. Password comparison constant-time con HMAC (no timing attacks). Token sessione 32 byte da `crypto.getRandomValues()` |
| **CAPTCHA** | Cloudflare Turnstile sul login. Niente bot che entrano nel pannello del bot |
| **Webhook validation** | Secret token su ogni richiesta da Telegram. Se non hai il token, vaffanculo |
| **SQL injection** | Zero. Tutte le query D1 usano `.bind()` con parametri. Mai una stringa concatenata |
| **Security headers** | CSP, HSTS, X-Frame-Options DENY, X-Content-Type-Options nosniff, Referrer-Policy strict, Permissions-Policy restrittiva |
| **security.txt** | `/.well-known/security.txt` con contatto per vulnerability disclosure. Responsible disclosure, come i grandi |
| **Secrets** | Tutti esternalizzati via `wrangler secret put` o Cloudflare Dashboard. Niente in codice o wrangler.toml. Mai. |
| **Path normalization** | Middleware normalizza double-slash e path traversal per prevenire auth bypass |
| **R2 key verification** | La key R2 viene verificata da D1, non dal client. Non puoi inventarti path a cazzo |
| **Session expiry** | 24h con cleanup automatico. Le sessioni morte vengono eliminate |
| **Upload validation** | MIME type e dimensione verificati server-side. Max 50MB |

---

## Sviluppo Locale

```bash
# Worker
cd worker
npx wrangler dev
# Il bot gira su http://localhost:8787
# Per testare con Telegram serve un tunnel (cloudflared, ngrok, ecc.)

# Backoffice
cd backoffice
npm run dev
# Gira su http://localhost:4321
```

### Dev con tunnel (per testare il webhook)

```bash
# In un terminale
cd worker && npx wrangler dev

# In un altro terminale
cloudflared tunnel --url http://localhost:8787

# Prendi l'URL del tunnel e registra il webhook
curl -H "Authorization: Bearer TUO_BOT_SECRET" "https://TUNNEL-URL/register"
```

---

## Performance e Caching

| Aspetto | Come funziona |
|---------|---------------|
| **Cloudflare Workers** | Il bot gira all'edge, latenza bassissima ovunque nel mondo. Piu veloce della tua connessione |
| **D1** | SQLite distribuito. Query veloci per contenuti random (`ORDER BY RANDOM() LIMIT 1`). Niente cold start |
| **telegram_file_id** | Dopo il primo invio di un media, il bot salva l'ID Telegram. Le volte successive usa l'ID invece di ri-scaricare da R2. Risparmio di banda e tempo |
| **R2** | Storage S3-compatible a basso costo. Zero egress fees. Puoi caricare tonnellate di audio porno senza pagare un cazzo di traffico |
| **Cron ottimizzato** | Due cron separati: ogni minuto per i broadcast, giornaliero per la pulizia log. Niente spreco |

---

## Numeri che contano

| Cosa | Quanto |
|------|--------|
| Comandi totali | 50+ |
| Categorie contenuti | 40+ |
| Frasi pre-caricate | 3000+ |
| Tabelle database | 9 |
| File di migrazione | 16 |
| Righe di migrazioni | ~4000 |
| Categorie audio | 7 |
| Categorie foto | 4 |
| Categorie sticker | 3 |
| Template meteo | 64 (8 categorie x 8 template) |
| Segni zodiacali | 12 (tutti insultati equamente) |
| Squadre insultate | 4 (Juve, Roma, Lazio, Milan) |
| API endpoints backoffice | 15+ |
| Reazioni emoji automatiche | 7 tipi |

---

## Contribuire

Vuoi aggiungere features? Fork, branch, PR. Le solite cose.

Se vuoi aggiungere contenuti, il modo piu veloce e usare il bulk import dal backoffice (fino a 500 frasi per volta). Se vuoi aggiungere un nuovo comando, guarda `worker/src/commands/text.ts` e segui il pattern. E piu facile che scopare.

---

## Disclaimer

SborrBot e un progetto di intrattenimento. Il bot e pensato per essere usato tra amici in chat private/di gruppo per fare cazzate e ridere. Non siamo responsabili se:

- Il tuo prete ti scomunica dopo aver letto le bestemmie
- Tua madre legge la chat e ti toglie il WiFi
- Qualcuno si offende per un insulto generato da un bot
- Telegram ti banna perche hai uploadato troppo porno
- La tua ragazza legge il profilo Tinder che il bot ti ha generato
- I Carabinieri bussano alla porta per le minacce (sono finte, giuro)
- Colleghi vedono l'esorcismo che hai fatto fare al capo
- Il tuo psicologo ti chiede perche il bot sa gia che vai in terapia

**Usalo con criterio.** O senza, chissenefotte.

---

## Licenza

MIT License — fai quello cazzo che vuoi. Vedi [LICENSE](LICENSE).

---

*Fatto con amore, bestemmie, 3000+ frasi di insulti e tanto caffe. Powered by Cloudflare Workers, D1 e R2. Infrastruttura enterprise al servizio della cazzata.*
