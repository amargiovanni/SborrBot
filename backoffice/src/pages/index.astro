---
// Landing page - pubblica, senza autenticazione
---

<html lang="it">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>SborrBot - Il bot Telegram più sborrante del web</title>
  <meta name="description" content="SborrBot: insulti personalizzati, oroscopi osceni, fake news sui tuoi amici e molto altro. Aggiungilo al tuo gruppo Telegram. Open source." />
  <meta property="og:title" content="SborrBot - Il bot Telegram più cattivo del web" />
  <meta property="og:description" content="Insulti, bestemmie, oroscopi osceni e fake news. Gratis. Open source. Aggiungi SborrBot al tuo gruppo." />
  <meta property="og:image" content="/sborrbot.jpeg" />
  <meta name="theme-color" content="#04060f" media="(prefers-color-scheme: dark)" />
  <meta name="theme-color" content="#f8fafc" media="(prefers-color-scheme: light)" />
  <link rel="icon" type="image/svg+xml" href="data:image/svg+xml,<svg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 100 100'><text y='.9em' font-size='90'>💀</text></svg>" />
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link href="https://fonts.googleapis.com/css2?family=Anton&family=DM+Mono:wght@300;400;500&family=DM+Sans:opsz,wght@9..40,300;9..40,400;9..40,500;9..40,600;9..40,700&display=swap" rel="stylesheet" />
</head>
<body>

<!-- ═══════════════════════ NAVBAR ═══════════════════════ -->
<nav class="navbar" id="navbar">
  <div class="nav-inner">
    <a href="/" class="nav-logo">
      <img src="/sborrbot.jpeg" alt="" class="nav-avatar" />
      <span class="nav-brand">SborrBot</span>
    </a>
    <div class="nav-links">
      <a href="https://github.com/amargiovanni/SborrBot" target="_blank" rel="noopener" class="nav-link gh-link">
        <svg width="18" height="18" viewBox="0 0 24 24" fill="currentColor"><path d="M12 0C5.37 0 0 5.37 0 12c0 5.31 3.435 9.795 8.205 11.385.6.105.825-.255.825-.57 0-.285-.015-1.23-.015-2.235-3.015.555-3.795-.735-4.035-1.41-.135-.345-.72-1.41-1.23-1.695-.42-.225-1.02-.78-.015-.795.945-.015 1.62.87 1.845 1.23 1.08 1.815 2.805 1.305 3.495.99.105-.78.42-1.305.765-1.605-2.67-.3-5.46-1.335-5.46-5.925 0-1.305.465-2.385 1.23-3.225-.12-.3-.54-1.53.12-3.18 0 0 1.005-.315 3.3 1.23.96-.27 1.98-.405 3-.405s2.04.135 3 .405c2.295-1.56 3.3-1.23 3.3-1.23.66 1.65.24 2.88.12 3.18.765.84 1.23 1.905 1.23 3.225 0 4.605-2.805 5.625-5.475 5.925.435.375.81 1.095.81 2.22 0 1.605-.015 2.895-.015 3.3 0 .315.225.69.825.57A12.02 12.02 0 0024 12c0-6.63-5.37-12-12-12z"/></svg>
        <span>GitHub</span>
      </a>
      <a href="/dashboard" class="nav-btn">
        Dashboard
        <span class="nav-btn-arrow">&rarr;</span>
      </a>
    </div>
  </div>
</nav>

<!-- ═══════════════════════ HERO ═══════════════════════ -->
<section class="hero">
  <div class="hero-bg"></div>
  <div class="scanlines"></div>
  <div class="particles" aria-hidden="true">
    <span></span><span></span><span></span><span></span>
    <span></span><span></span><span></span><span></span>
    <span></span><span></span><span></span><span></span>
    <span></span><span></span><span></span><span></span>
  </div>

  <div class="hero-content">
    <div class="avatar-wrap">
      <div class="avatar-ring"></div>
      <div class="avatar-ring ring2"></div>
      <img src="/sborrbot.jpeg" alt="SborrBot" class="avatar" />
    </div>

    <div class="name-wrap">
      <h1 class="bot-name" data-text="SborrBot">SborrBot</h1>
      <div class="name-underline"></div>
    </div>

    <p class="bot-sub">
      <span class="typing-text">il bot Telegram più cattivo del web</span><span class="cursor-blink">█</span>
    </p>

    <p class="hero-desc">
      Insulti su misura, oroscopi osceni, fake news sui tuoi amici,
      bestemmie fresche e molto altro. Gratis. Perché siamo stronzi generosi.
    </p>

    <div class="hero-ctas">
      <a href="https://t.me/SborrrrBot_bot?startgroup=true" target="_blank" rel="noopener" class="cta-primary">
        <svg width="22" height="22" viewBox="0 0 24 24" fill="currentColor"><path d="M12 0C5.373 0 0 5.373 0 12s5.373 12 12 12 12-5.373 12-12S18.627 0 12 0zm5.562 8.248l-2.018 9.51c-.148.658-.537.818-1.084.508l-3-2.21-1.447 1.394c-.16.16-.295.295-.605.295l.213-3.053 5.56-5.023c.242-.213-.054-.333-.373-.12L7.116 14.37 4.15 13.45c-.65-.203-.662-.65.136-.963l10.892-4.197c.54-.195 1.016.12.84.958h-.456z"/></svg>
        <span>Aggiungi al tuo gruppo</span>
        <span class="cta-arrow">&rarr;</span>
      </a>
      <a href="https://github.com/amargiovanni/SborrBot" target="_blank" rel="noopener" class="cta-secondary">
        <svg width="20" height="20" viewBox="0 0 24 24" fill="currentColor"><path d="M12 0C5.37 0 0 5.37 0 12c0 5.31 3.435 9.795 8.205 11.385.6.105.825-.255.825-.57 0-.285-.015-1.23-.015-2.235-3.015.555-3.795-.735-4.035-1.41-.135-.345-.72-1.41-1.23-1.695-.42-.225-1.02-.78-.015-.795.945-.015 1.62.87 1.845 1.23 1.08 1.815 2.805 1.305 3.495.99.105-.78.42-1.305.765-1.605-2.67-.3-5.46-1.335-5.46-5.925 0-1.305.465-2.385 1.23-3.225-.12-.3-.54-1.53.12-3.18 0 0 1.005-.315 3.3 1.23.96-.27 1.98-.405 3-.405s2.04.135 3 .405c2.295-1.56 3.3-1.23 3.3-1.23.66 1.65.24 2.88.12 3.18.765.84 1.23 1.905 1.23 3.225 0 4.605-2.805 5.625-5.475 5.925.435.375.81 1.095.81 2.22 0 1.605-.015 2.895-.015 3.3 0 .315.225.69.825.57A12.02 12.02 0 0024 12c0-6.63-5.37-12-12-12z"/></svg>
        <span>Codice sorgente</span>
      </a>
    </div>

    <div class="hero-stats">
      <div class="stat">
        <span class="stat-num">40+</span>
        <span class="stat-label">comandi</span>
      </div>
      <div class="stat-sep"></div>
      <div class="stat">
        <span class="stat-num">100%</span>
        <span class="stat-label">open source</span>
      </div>
      <div class="stat-sep"></div>
      <div class="stat">
        <span class="stat-num">0%</span>
        <span class="stat-label">filtri decenza</span>
      </div>
    </div>
  </div>

  <div class="scroll-hint" aria-hidden="true">
    <span class="scroll-text">scorri per scoprire le feature</span>
    <div class="scroll-arrow">
      <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M12 5v14M5 12l7 7 7-7"/></svg>
    </div>
  </div>
</section>

<!-- ═══════════════════════ FEATURES ═══════════════════════ -->
<section class="features" id="features">
  <div class="section-inner">
    <div class="section-header">
      <span class="section-badge">COSA FA 'STO COSO</span>
      <h2 class="section-title">Un arsenale di cattiveria</h2>
      <p class="section-desc">Tutto quello che ti serve per rendere il tuo gruppo Telegram un posto peggiore.</p>
    </div>

    <div class="feat-grid">
      <div class="feat-card feat-card-lg">
        <div class="feat-icon-wrap"><span class="feat-icon">💀</span></div>
        <h3 class="feat-name">Insulti personalizzati</h3>
        <p class="feat-desc">Scrivi <code>insulta Mario</code> e il bot genera un insulto unico, fresco e devastante. Con <code>insulta combo</code> ne spara tre di fila. FATALITY.</p>
        <div class="feat-tags">
          <span class="feat-tag">insulta [nome]</span>
          <span class="feat-tag">insulta combo [nome]</span>
        </div>
      </div>

      <div class="feat-card">
        <div class="feat-icon-wrap"><span class="feat-icon">🔮</span></div>
        <h3 class="feat-name">Oroscopi osceni</h3>
        <p class="feat-desc">Le stelle dicono che sei un coglione. Oroscopo personalizzato per segno zodiacale, zero filtri.</p>
        <div class="feat-tags">
          <span class="feat-tag">oroscopo ariete</span>
        </div>
      </div>

      <div class="feat-card">
        <div class="feat-icon-wrap"><span class="feat-icon">📰</span></div>
        <h3 class="feat-name">Fake news flash</h3>
        <p class="feat-desc">Breaking news su un membro random del gruppo. Notizie inventate ma stranamente plausibili.</p>
        <div class="feat-tags">
          <span class="feat-tag">notizia</span>
          <span class="feat-tag">segreto</span>
        </div>
      </div>

      <div class="feat-card">
        <div class="feat-icon-wrap"><span class="feat-icon">⚰️</span></div>
        <h3 class="feat-name">Necrologi pre-mortem</h3>
        <p class="feat-desc">Perche aspettare che muori? Il bot scrive il tuo necrologio ed elogio funebre adesso.</p>
        <div class="feat-tags">
          <span class="feat-tag">necrologio [nome]</span>
          <span class="feat-tag">autopsia [nome]</span>
        </div>
      </div>

      <div class="feat-card">
        <div class="feat-icon-wrap"><span class="feat-icon">🩺</span></div>
        <h3 class="feat-name">Diagnosi mediche</h3>
        <p class="feat-desc">Referti medici assurdi e completamente inventati. La sanita pubblica peggiore che tu abbia mai visto.</p>
        <div class="feat-tags">
          <span class="feat-tag">diagnosi [nome]</span>
          <span class="feat-tag">cv [nome]</span>
        </div>
      </div>

      <div class="feat-card">
        <div class="feat-icon-wrap"><span class="feat-icon">😈</span></div>
        <h3 class="feat-name">Bestemmie & saggezza</h3>
        <p class="feat-desc">Bestemmie fresche, citazioni di nonno ubriaco e perle di saggezza che non trovi su nessun libro.</p>
        <div class="feat-tags">
          <span class="feat-tag">bestemmia</span>
          <span class="feat-tag">come diceva mio nonno</span>
        </div>
      </div>

      <div class="feat-card">
        <div class="feat-icon-wrap"><span class="feat-icon">⛅</span></div>
        <h3 class="feat-name">Meteo volgare</h3>
        <p class="feat-desc">Previsioni meteo reali (da OpenWeatherMap) ma commentate come solo SborrBot sa fare.</p>
        <div class="feat-tags">
          <span class="feat-tag">meteo Roma</span>
        </div>
      </div>

      <div class="feat-card">
        <div class="feat-icon-wrap"><span class="feat-icon">🗣️</span></div>
        <h3 class="feat-name">Audio epici</h3>
        <p class="feat-desc">Germano Mosconi, Richard Benson, Christian De Sica, Homer Simpson. Le voci sacre del trash italiano.</p>
        <div class="feat-tags">
          <span class="feat-tag">germano mosconi</span>
          <span class="feat-tag">richard benson</span>
        </div>
      </div>

      <div class="feat-card">
        <div class="feat-icon-wrap"><span class="feat-icon">💩</span></div>
        <h3 class="feat-name">Anti-Juve automatico</h3>
        <p class="feat-desc">Qualcuno scrive "Juve" nel gruppo? Il bot reagisce istantaneamente. Perche i gobbi vanno puniti.</p>
        <div class="feat-tags">
          <span class="feat-tag">auto-trigger</span>
        </div>
      </div>

      <div class="feat-card">
        <div class="feat-icon-wrap"><span class="feat-icon">🍝</span></div>
        <h3 class="feat-name">Ricette & complotti</h3>
        <p class="feat-desc">Ricette disgustose dello chef, teorie del complotto su membri del gruppo e testamenti assurdi.</p>
        <div class="feat-tags">
          <span class="feat-tag">ricetta</span>
          <span class="feat-tag">complotto</span>
          <span class="feat-tag">eredita</span>
        </div>
      </div>
    </div>
  </div>
</section>

<!-- ═══════════════════════ HOW IT WORKS ═══════════════════════ -->
<section class="how-it-works">
  <div class="section-inner">
    <div class="section-header">
      <span class="section-badge">COME FUNZIONA</span>
      <h2 class="section-title">Tre passi verso il degrado</h2>
    </div>

    <div class="steps">
      <div class="step">
        <div class="step-num">1</div>
        <h3 class="step-title">Aggiungi il bot al gruppo</h3>
        <p class="step-desc">Clicca il pulsante, aggiungi SborrBot al tuo gruppo Telegram e dagli i permessi per leggere e scrivere messaggi.</p>
      </div>
      <div class="step-connector"><span></span></div>
      <div class="step">
        <div class="step-num">2</div>
        <h3 class="step-title">Scrivi un comando</h3>
        <p class="step-desc">Non serve nessuna slash. Scrivi <code>insulta Mario</code> o <code>oroscopo leone</code> direttamente in chat, come un messaggio normale.</p>
      </div>
      <div class="step-connector"><span></span></div>
      <div class="step">
        <div class="step-num">3</div>
        <h3 class="step-title">Goditi il caos</h3>
        <p class="step-desc">Il bot risponde istantaneamente con testi, audio, foto o sticker. Nessuna censura. Nessun filtro. Nessun rimorso.</p>
      </div>
    </div>
  </div>
</section>

<!-- ═══════════════════════ OPEN SOURCE ═══════════════════════ -->
<section class="opensource">
  <div class="section-inner">
    <div class="os-card">
      <div class="os-content">
        <span class="section-badge">OPEN SOURCE</span>
        <h2 class="os-title">Il codice e tutto tuo,<br/>nerd di merda.</h2>
        <p class="os-desc">
          SborrBot e 100% open source. Puoi leggere ogni singola riga di codice, forkarlo,
          modificarlo e hostarlo da solo. Non spiamo un cazzo &mdash; il codice parla da se.
        </p>
        <div class="os-stack">
          <div class="stack-item">
            <span class="stack-icon">
              <svg width="20" height="20" viewBox="0 0 128 128"><path fill="currentColor" d="M64.004 25.602c-17.067 0-27.73 8.535-32 25.603 6.398-8.534 13.867-11.733 22.398-9.597 4.871 1.22 8.352 4.762 12.207 8.685C72.883 56.629 80.145 64 96.004 64c17.066 0 27.73-8.535 32-25.602-6.399 8.534-13.867 11.733-22.399 9.597-4.87-1.22-8.352-4.762-12.207-8.685-6.273-6.336-13.535-13.71-29.394-13.71zM32.004 64c-17.066 0-27.73 8.535-32 25.602 6.399-8.534 13.867-11.733 22.399-9.597 4.87 1.22 8.352 4.762 12.207 8.685C40.883 95.025 48.145 102.4 64.004 102.4c17.066 0 27.73-8.536 32-25.603-6.399 8.534-13.867 11.733-22.399 9.598-4.87-1.22-8.352-4.763-12.207-8.686C55.125 71.373 47.863 64 32.004 64z"/></svg>
            </span>
            <span>Tailwind CSS</span>
          </div>
          <div class="stack-item">
            <span class="stack-icon">CF</span>
            <span>Cloudflare Workers</span>
          </div>
          <div class="stack-item">
            <span class="stack-icon">D1</span>
            <span>Cloudflare D1</span>
          </div>
          <div class="stack-item">
            <span class="stack-icon">R2</span>
            <span>Cloudflare R2</span>
          </div>
          <div class="stack-item">
            <span class="stack-icon">
              <svg width="20" height="20" viewBox="0 0 24 24" fill="currentColor"><path d="M12 0C5.37 0 0 5.37 0 12c0 5.31 3.435 9.795 8.205 11.385.6.105.825-.255.825-.57 0-.285-.015-1.23-.015-2.235-3.015.555-3.795-.735-4.035-1.41-.135-.345-.72-1.41-1.23-1.695-.42-.225-1.02-.78-.015-.795.945-.015 1.62.87 1.845 1.23 1.08 1.815 2.805 1.305 3.495.99.105-.78.42-1.305.765-1.605-2.67-.3-5.46-1.335-5.46-5.925 0-1.305.465-2.385 1.23-3.225-.12-.3-.54-1.53.12-3.18 0 0 1.005-.315 3.3 1.23.96-.27 1.98-.405 3-.405s2.04.135 3 .405c2.295-1.56 3.3-1.23 3.3-1.23.66 1.65.24 2.88.12 3.18.765.84 1.23 1.905 1.23 3.225 0 4.605-2.805 5.625-5.475 5.925.435.375.81 1.095.81 2.22 0 1.605-.015 2.895-.015 3.3 0 .315.225.69.825.57A12.02 12.02 0 0024 12c0-6.63-5.37-12-12-12z"/></svg>
            </span>
            <span>TypeScript</span>
          </div>
        </div>
        <a href="https://github.com/amargiovanni/SborrBot" target="_blank" rel="noopener" class="os-btn">
          <svg width="20" height="20" viewBox="0 0 24 24" fill="currentColor"><path d="M12 0C5.37 0 0 5.37 0 12c0 5.31 3.435 9.795 8.205 11.385.6.105.825-.255.825-.57 0-.285-.015-1.23-.015-2.235-3.015.555-3.795-.735-4.035-1.41-.135-.345-.72-1.41-1.23-1.695-.42-.225-1.02-.78-.015-.795.945-.015 1.62.87 1.845 1.23 1.08 1.815 2.805 1.305 3.495.99.105-.78.42-1.305.765-1.605-2.67-.3-5.46-1.335-5.46-5.925 0-1.305.465-2.385 1.23-3.225-.12-.3-.54-1.53.12-3.18 0 0 1.005-.315 3.3 1.23.96-.27 1.98-.405 3-.405s2.04.135 3 .405c2.295-1.56 3.3-1.23 3.3-1.23.66 1.65.24 2.88.12 3.18.765.84 1.23 1.905 1.23 3.225 0 4.605-2.805 5.625-5.475 5.925.435.375.81 1.095.81 2.22 0 1.605-.015 2.895-.015 3.3 0 .315.225.69.825.57A12.02 12.02 0 0024 12c0-6.63-5.37-12-12-12z"/></svg>
          <span>Vedi su GitHub</span>
          <span class="os-btn-arrow">&rarr;</span>
        </a>
      </div>
      <div class="os-visual">
        <div class="code-window">
          <div class="code-bar">
            <span class="dot red"></span>
            <span class="dot yellow"></span>
            <span class="dot green"></span>
            <span class="code-title">bot.ts</span>
          </div>
          <pre class="code-body"><code set:html={`<span class="c-comment">// SborrBot - il bot che non hai chiesto</span>
<span class="c-keyword">const</span> <span class="c-var">bot</span> = <span class="c-keyword">new</span> <span class="c-type">TelegramBot</span>({
  <span class="c-prop">name</span>: <span class="c-string">"SborrBot"</span>,
  <span class="c-prop">censura</span>: <span class="c-bool">false</span>,
  <span class="c-prop">pietà</span>: <span class="c-bool">false</span>,
  <span class="c-prop">vergogna</span>: <span class="c-bool">false</span>,
  <span class="c-prop">filtri</span>: <span class="c-bool">null</span>,
});

<span class="c-var">bot</span>.<span class="c-func">on</span>(<span class="c-string">"insulta"</span>, (ctx) =&gt; {
  ctx.<span class="c-func">reply</span>(<span class="c-func">generateInsulto</span>(
    ctx.<span class="c-prop">target</span>
  ));
});`} /></pre>
        </div>
      </div>
    </div>
  </div>
</section>

<!-- ═══════════════════════ FINAL CTA ═══════════════════════ -->
<section class="final-cta">
  <div class="section-inner">
    <div class="cta-content">
      <h2 class="cta-title">Pronto a rovinare<br/>il tuo gruppo?</h2>
      <p class="cta-desc">Non ti promettiamo che sara un'esperienza piacevole. Ti promettiamo che sara memorabile.</p>
      <a href="https://t.me/SborrrrBot_bot?startgroup=true" target="_blank" rel="noopener" class="cta-primary cta-big">
        <svg width="24" height="24" viewBox="0 0 24 24" fill="currentColor"><path d="M12 0C5.373 0 0 5.373 0 12s5.373 12 12 12 12-5.373 12-12S18.627 0 12 0zm5.562 8.248l-2.018 9.51c-.148.658-.537.818-1.084.508l-3-2.21-1.447 1.394c-.16.16-.295.295-.605.295l.213-3.053 5.56-5.023c.242-.213-.054-.333-.373-.12L7.116 14.37 4.15 13.45c-.65-.203-.662-.65.136-.963l10.892-4.197c.54-.195 1.016.12.84.958h-.456z"/></svg>
        <span>Aggiungi SborrBot al tuo gruppo</span>
        <span class="cta-arrow">&rarr;</span>
      </a>
    </div>
  </div>
</section>

<!-- ═══════════════════════ FOOTER ═══════════════════════ -->
<footer class="footer">
  <div class="section-inner">
    <div class="footer-inner">
      <div class="footer-left">
        <span class="footer-brand">SborrBot</span>
        <span class="footer-sep">&middot;</span>
        <span class="footer-copy">Fatto con cattiveria e zero buon gusto</span>
      </div>
      <div class="footer-links">
        <a href="https://t.me/SborrrrBot_bot" target="_blank" rel="noopener">Telegram</a>
        <a href="https://github.com/amargiovanni/SborrBot" target="_blank" rel="noopener">GitHub</a>
        <a href="/dashboard">Dashboard</a>
      </div>
    </div>
    <div class="footer-powered">
      Powered by Cloudflare Workers &middot; D1 &middot; R2 &middot; Astro
    </div>
  </div>
</footer>

<!-- ═══════════════════════ STYLES ═══════════════════════ -->
<style>
  @import url('https://fonts.googleapis.com/css2?family=Anton&family=DM+Mono:wght@300;400;500&family=DM+Sans:opsz,wght@9..40,300;9..40,400;9..40,500;9..40,600;9..40,700&display=swap');

  *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

  :root {
    --bg: #f8fafc;
    --bg-alt: #f1f5f9;
    --bg-card: #ffffff;
    --text: #0f172a;
    --text-muted: #64748b;
    --text-faint: #94a3b8;
    --border: #e2e8f0;
    --primary: #2563eb;
    --primary-light: #3b82f6;
    --primary-bg: rgba(37, 99, 235, 0.06);
    --primary-border: rgba(37, 99, 235, 0.15);
    --code-bg: #f1f5f9;
  }

  @media (prefers-color-scheme: dark) {
    :root {
      --bg: #04060f;
      --bg-alt: #0a0e1a;
      --bg-card: #0d1117;
      --text: #f1f5f9;
      --text-muted: #94a3b8;
      --text-faint: #475569;
      --border: #1e293b;
      --primary: #3b82f6;
      --primary-light: #60a5fa;
      --primary-bg: rgba(37, 99, 235, 0.08);
      --primary-border: rgba(37, 99, 235, 0.2);
      --code-bg: #161d2b;
    }
  }

  html { scroll-behavior: smooth; }

  body {
    font-family: 'DM Sans', system-ui, sans-serif;
    background: var(--bg);
    color: var(--text);
    -webkit-font-smoothing: antialiased;
    overflow-x: hidden;
  }

  code {
    font-family: 'DM Mono', monospace;
    font-size: 0.82em;
    background: var(--code-bg);
    padding: 2px 6px;
    border-radius: 4px;
    color: var(--primary-light);
  }

  .section-inner {
    max-width: 1120px;
    margin: 0 auto;
    padding: 0 1.5rem;
  }

  /* ─────────────────── NAVBAR ─────────────────── */

  .navbar {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    z-index: 100;
    padding: 0 1.5rem;
    transition: background 0.3s, backdrop-filter 0.3s, box-shadow 0.3s;
  }

  .navbar.scrolled {
    background: rgba(248, 250, 252, 0.85);
    backdrop-filter: blur(16px);
    -webkit-backdrop-filter: blur(16px);
    box-shadow: 0 1px 0 var(--border);
  }

  @media (prefers-color-scheme: dark) {
    .navbar.scrolled {
      background: rgba(4, 6, 15, 0.85);
    }
  }

  .nav-inner {
    max-width: 1120px;
    margin: 0 auto;
    display: flex;
    align-items: center;
    justify-content: space-between;
    height: 64px;
  }

  .nav-logo {
    display: flex;
    align-items: center;
    gap: 0.6rem;
    text-decoration: none;
    color: var(--text);
  }

  .nav-avatar {
    width: 32px;
    height: 32px;
    border-radius: 50%;
    object-fit: cover;
    border: 2px solid var(--primary-border);
  }

  .nav-brand {
    font-family: 'Anton', sans-serif;
    font-size: 1.2rem;
    letter-spacing: 0.02em;
  }

  .nav-links {
    display: flex;
    align-items: center;
    gap: 0.75rem;
  }

  .nav-link {
    display: flex;
    align-items: center;
    gap: 0.4rem;
    text-decoration: none;
    font-size: 0.82rem;
    font-weight: 500;
    color: var(--text-muted);
    padding: 0.4rem 0.8rem;
    border-radius: 8px;
    transition: color 0.15s, background 0.15s;
  }

  .nav-link:hover {
    color: var(--text);
    background: var(--primary-bg);
  }

  .nav-btn {
    display: flex;
    align-items: center;
    gap: 0.4rem;
    text-decoration: none;
    font-size: 0.82rem;
    font-weight: 600;
    color: white;
    background: var(--primary);
    padding: 0.5rem 1rem;
    border-radius: 8px;
    transition: background 0.15s, transform 0.1s, box-shadow 0.15s;
    box-shadow: 0 2px 8px rgba(37, 99, 235, 0.3);
  }

  .nav-btn:hover {
    background: #1d4ed8;
    transform: translateY(-1px);
    box-shadow: 0 4px 14px rgba(37, 99, 235, 0.4);
  }

  .nav-btn-arrow {
    transition: transform 0.2s;
  }

  .nav-btn:hover .nav-btn-arrow {
    transform: translateX(3px);
  }

  /* ─────────────────── HERO ─────────────────── */

  .hero {
    position: relative;
    min-height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
    overflow: hidden;
    background: var(--bg);
  }

  @media (prefers-color-scheme: dark) {
    .hero { background: #04060f; }
  }

  .hero-bg {
    position: absolute;
    inset: 0;
    background-image:
      linear-gradient(rgba(37, 99, 235, 0.04) 1px, transparent 1px),
      linear-gradient(90deg, rgba(37, 99, 235, 0.04) 1px, transparent 1px);
    background-size: 44px 44px;
    animation: gridDrift 25s linear infinite;
  }

  @media (prefers-color-scheme: dark) {
    .hero-bg {
      background-image:
        linear-gradient(rgba(37, 99, 235, 0.055) 1px, transparent 1px),
        linear-gradient(90deg, rgba(37, 99, 235, 0.055) 1px, transparent 1px);
    }
  }

  @keyframes gridDrift {
    from { background-position: 0 0; }
    to { background-position: 44px 44px; }
  }

  .scanlines {
    position: absolute;
    inset: 0;
    pointer-events: none;
    background: repeating-linear-gradient(to bottom, transparent, transparent 2px, rgba(0,0,0,0.03) 2px, rgba(0,0,0,0.03) 4px);
    z-index: 1;
    display: none;
  }

  @media (prefers-color-scheme: dark) {
    .scanlines {
      display: block;
      background: repeating-linear-gradient(to bottom, transparent, transparent 2px, rgba(0,0,0,0.13) 2px, rgba(0,0,0,0.13) 4px);
    }
  }

  .particles { position: absolute; inset: 0; pointer-events: none; z-index: 1; display: none; }
  @media (prefers-color-scheme: dark) { .particles { display: block; } }

  .particles span {
    position: absolute; bottom: -10px; border-radius: 50%; background: #3b82f6; opacity: 0;
    animation: floatUp var(--dur, 6s) var(--delay, 0s) infinite ease-in;
    width: var(--sz, 3px); height: var(--sz, 3px);
  }

  .particles span:nth-child(1)  { left: 4%;  --dur: 7s;   --delay: 0s;   --sz: 2px; background: #60a5fa; }
  .particles span:nth-child(2)  { left: 11%; --dur: 5.5s; --delay: 1.5s; --sz: 4px; }
  .particles span:nth-child(3)  { left: 19%; --dur: 8s;   --delay: 0.7s; --sz: 2px; background: #93c5fd; }
  .particles span:nth-child(4)  { left: 27%; --dur: 6.5s; --delay: 2.2s; --sz: 3px; }
  .particles span:nth-child(5)  { left: 34%; --dur: 9s;   --delay: 0.3s; --sz: 2px; background: #60a5fa; }
  .particles span:nth-child(6)  { left: 41%; --dur: 5s;   --delay: 1s;   --sz: 4px; }
  .particles span:nth-child(7)  { left: 49%; --dur: 7.5s; --delay: 3.2s; --sz: 2px; background: #93c5fd; }
  .particles span:nth-child(8)  { left: 57%; --dur: 6s;   --delay: 0.6s; --sz: 3px; }
  .particles span:nth-child(9)  { left: 64%; --dur: 8.5s; --delay: 2s;   --sz: 2px; }
  .particles span:nth-child(10) { left: 71%; --dur: 5.5s; --delay: 1.8s; --sz: 4px; background: #60a5fa; }
  .particles span:nth-child(11) { left: 79%; --dur: 7s;   --delay: 0.4s; --sz: 2px; }
  .particles span:nth-child(12) { left: 87%; --dur: 6s;   --delay: 2.6s; --sz: 3px; background: #93c5fd; }
  .particles span:nth-child(13) { left: 93%; --dur: 9s;   --delay: 1.2s; --sz: 2px; }
  .particles span:nth-child(14) { left: 16%; --dur: 6s;   --delay: 4s;   --sz: 3px; background: #1d4ed8; }
  .particles span:nth-child(15) { left: 47%; --dur: 8s;   --delay: 3.5s; --sz: 2px; background: #60a5fa; }
  .particles span:nth-child(16) { left: 74%; --dur: 5.5s; --delay: 0.9s; --sz: 4px; }

  @keyframes floatUp {
    0%   { transform: translateY(0) scale(1); opacity: 0; }
    8%   { opacity: 0.9; }
    90%  { opacity: 0.5; }
    100% { transform: translateY(-105vh) scale(0.4); opacity: 0; }
  }

  .hero-content {
    position: relative;
    z-index: 2;
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    padding: 6rem 1.5rem 3rem;
    max-width: 700px;
    width: 100%;
  }

  /* Avatar */
  .avatar-wrap {
    position: relative;
    width: 140px;
    height: 140px;
    margin-bottom: 2rem;
    animation: avatarPop 1s cubic-bezier(0.16, 1, 0.3, 1) 0.3s both;
  }

  @keyframes avatarPop {
    from { transform: scale(0.4) rotate(-8deg); opacity: 0; }
    to { transform: scale(1) rotate(0deg); opacity: 1; }
  }

  .avatar {
    width: 140px;
    height: 140px;
    border-radius: 50%;
    object-fit: cover;
    position: relative;
    z-index: 2;
    border: 3px solid var(--primary-border);
    display: block;
  }

  @media (prefers-color-scheme: dark) {
    .avatar { border-color: rgba(37, 99, 235, 0.7); }
  }

  .avatar-ring {
    position: absolute;
    inset: -8px;
    border-radius: 50%;
    border: 2px solid var(--primary-border);
    animation: ringGlow 2.8s ease-in-out infinite;
    z-index: 1;
  }

  @media (prefers-color-scheme: dark) {
    .avatar-ring { border-color: rgba(37, 99, 235, 0.7); }
  }

  .ring2 {
    inset: -18px;
    border-width: 1.5px;
    animation-delay: 0.9s;
  }

  @media (prefers-color-scheme: dark) {
    .ring2 { border-color: rgba(37, 99, 235, 0.3); }
  }

  @keyframes ringGlow {
    0%, 100% { box-shadow: 0 0 10px rgba(37, 99, 235, 0.15), 0 0 28px rgba(37, 99, 235, 0.08); opacity: 1; }
    50% { box-shadow: 0 0 22px rgba(37, 99, 235, 0.4), 0 0 55px rgba(37, 99, 235, 0.15); opacity: 0.75; }
  }

  @media (prefers-color-scheme: dark) {
    @keyframes ringGlow {
      0%, 100% { box-shadow: 0 0 10px rgba(37, 99, 235, 0.5), 0 0 28px rgba(37, 99, 235, 0.25); opacity: 1; }
      50% { box-shadow: 0 0 22px rgba(37, 99, 235, 1), 0 0 55px rgba(37, 99, 235, 0.5), 0 0 90px rgba(37, 99, 235, 0.2); opacity: 0.75; }
    }
  }

  /* Title */
  .name-wrap {
    margin-bottom: 0.6rem;
    animation: titleRise 0.8s cubic-bezier(0.16, 1, 0.3, 1) 0.5s both;
  }

  @keyframes titleRise {
    from { transform: translateY(18px); opacity: 0; }
    to { transform: translateY(0); opacity: 1; }
  }

  .bot-name {
    font-family: 'Anton', sans-serif;
    font-size: clamp(3.5rem, 8vw, 6rem);
    color: var(--text);
    letter-spacing: 0.03em;
    line-height: 1;
    position: relative;
    display: inline-block;
  }

  .bot-name::before, .bot-name::after {
    content: attr(data-text);
    position: absolute;
    top: 0; left: 0;
    width: 100%; height: 100%;
    font-family: inherit; font-size: inherit; letter-spacing: inherit;
    pointer-events: none;
    display: none;
  }

  @media (prefers-color-scheme: dark) {
    .bot-name::before, .bot-name::after { display: block; }
    .bot-name::before { color: #60a5fa; animation: glitch1 5s infinite; clip-path: inset(0 0 100% 0); }
    .bot-name::after  { color: #f97316; animation: glitch2 5s infinite; clip-path: inset(100% 0 0 0); }
  }

  @keyframes glitch1 {
    0%, 82%, 100% { transform: translate(0); clip-path: inset(0 0 100% 0); opacity: 0; }
    84% { transform: translate(-5px, 2px); clip-path: inset(5% 0 85% 0); opacity: 1; }
    86% { transform: translate(4px, -2px); clip-path: inset(30% 0 60% 0); opacity: 1; }
    88% { transform: translate(-3px, 1px); clip-path: inset(55% 0 35% 0); opacity: 1; }
    90% { transform: translate(0); clip-path: inset(0 0 100% 0); opacity: 0; }
  }

  @keyframes glitch2 {
    0%, 85%, 100% { transform: translate(0); clip-path: inset(100% 0 0 0); opacity: 0; }
    87% { transform: translate(5px, -2px); clip-path: inset(75% 0 5% 0); opacity: 1; }
    89% { transform: translate(-4px, 1px); clip-path: inset(55% 0 30% 0); opacity: 1; }
    91% { transform: translate(0); clip-path: inset(100% 0 0 0); opacity: 0; }
  }

  .name-underline {
    height: 2px;
    background: linear-gradient(90deg, transparent, var(--primary), transparent);
    border-radius: 2px;
    animation: underlineExpand 0.7s cubic-bezier(0.16, 1, 0.3, 1) 1s both;
    transform-origin: center;
  }

  @keyframes underlineExpand {
    from { transform: scaleX(0); opacity: 0; }
    to { transform: scaleX(1); opacity: 1; }
  }

  /* Subtitle */
  .bot-sub {
    font-family: 'DM Mono', monospace;
    font-size: 0.9rem;
    color: var(--primary-light);
    margin-bottom: 1.5rem;
    display: flex;
    align-items: center;
    gap: 3px;
    animation: fadeUp 0.6s ease 1.1s both;
  }

  @keyframes fadeUp {
    from { opacity: 0; transform: translateY(6px); }
    to { opacity: 1; transform: translateY(0); }
  }

  .cursor-blink {
    color: var(--primary);
    font-size: 0.65rem;
    animation: blink 0.9s step-end infinite;
  }

  @keyframes blink {
    0%, 100% { opacity: 1; }
    50% { opacity: 0; }
  }

  .hero-desc {
    font-size: 1.05rem;
    line-height: 1.7;
    color: var(--text-muted);
    max-width: 520px;
    margin-bottom: 2rem;
    animation: fadeUp 0.6s ease 1.3s both;
  }

  /* CTAs */
  .hero-ctas {
    display: flex;
    gap: 0.75rem;
    flex-wrap: wrap;
    justify-content: center;
    margin-bottom: 2.5rem;
    animation: fadeUp 0.5s ease 1.5s both;
  }

  .cta-primary {
    display: flex;
    align-items: center;
    gap: 0.6rem;
    padding: 0.85rem 1.5rem;
    background: var(--primary);
    color: white;
    font-family: 'DM Sans', sans-serif;
    font-weight: 600;
    font-size: 0.95rem;
    text-decoration: none;
    border-radius: 12px;
    transition: background 0.15s, transform 0.1s, box-shadow 0.15s;
    box-shadow: 0 4px 16px rgba(37, 99, 235, 0.32);
  }

  .cta-primary:hover {
    background: #1d4ed8;
    transform: translateY(-2px);
    box-shadow: 0 6px 24px rgba(37, 99, 235, 0.48);
  }

  .cta-primary:active { transform: translateY(0); }

  .cta-arrow {
    transition: transform 0.2s;
    margin-left: 0.2rem;
  }

  .cta-primary:hover .cta-arrow { transform: translateX(4px); }

  .cta-secondary {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.85rem 1.5rem;
    background: var(--primary-bg);
    border: 1.5px solid var(--primary-border);
    color: var(--primary-light);
    font-family: 'DM Sans', sans-serif;
    font-weight: 600;
    font-size: 0.95rem;
    text-decoration: none;
    border-radius: 12px;
    transition: background 0.15s, border-color 0.15s, transform 0.1s;
  }

  .cta-secondary:hover {
    background: rgba(37, 99, 235, 0.12);
    border-color: rgba(37, 99, 235, 0.35);
    transform: translateY(-2px);
  }

  /* Stats */
  .hero-stats {
    display: flex;
    align-items: center;
    gap: 1.5rem;
    animation: fadeUp 0.5s ease 1.7s both;
  }

  .stat { text-align: center; }

  .stat-num {
    display: block;
    font-family: 'Anton', sans-serif;
    font-size: 1.5rem;
    color: var(--text);
    letter-spacing: 0.02em;
  }

  .stat-label {
    font-size: 0.7rem;
    color: var(--text-faint);
    font-family: 'DM Mono', monospace;
    text-transform: uppercase;
    letter-spacing: 0.08em;
  }

  .stat-sep {
    width: 1px;
    height: 28px;
    background: var(--border);
  }

  /* Scroll hint */
  .scroll-hint {
    position: absolute;
    bottom: 2rem;
    left: 0;
    right: 0;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.5rem;
    animation: fadeUp 0.5s ease 2s both;
    z-index: 2;
  }

  .scroll-text {
    font-family: 'DM Mono', monospace;
    font-size: 0.65rem;
    color: var(--text-faint);
    letter-spacing: 0.06em;
    text-transform: uppercase;
  }

  .scroll-arrow {
    color: var(--text-faint);
    animation: bounceDown 2s ease-in-out infinite;
  }

  @keyframes bounceDown {
    0%, 100% { transform: translateY(0); }
    50% { transform: translateY(6px); }
  }

  /* ─────────────────── FEATURES ─────────────────── */

  .features {
    padding: 6rem 0;
    background: var(--bg-alt);
  }

  .section-header {
    text-align: center;
    margin-bottom: 3.5rem;
  }

  .section-badge {
    display: inline-block;
    font-family: 'DM Mono', monospace;
    font-size: 0.62rem;
    font-weight: 500;
    letter-spacing: 0.18em;
    color: var(--primary);
    background: var(--primary-bg);
    border: 1px solid var(--primary-border);
    border-radius: 4px;
    padding: 4px 10px;
    margin-bottom: 1rem;
  }

  .section-title {
    font-family: 'Anton', sans-serif;
    font-size: clamp(2rem, 4vw, 3rem);
    letter-spacing: 0.02em;
    color: var(--text);
    line-height: 1.1;
    margin-bottom: 0.75rem;
  }

  .section-desc {
    font-size: 1rem;
    color: var(--text-muted);
    max-width: 500px;
    margin: 0 auto;
    line-height: 1.6;
  }

  .feat-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 1rem;
  }

  .feat-card {
    background: var(--bg-card);
    border: 1px solid var(--border);
    border-radius: 16px;
    padding: 1.5rem;
    transition: transform 0.2s, box-shadow 0.2s, border-color 0.2s;
  }

  .feat-card:hover {
    transform: translateY(-4px);
    box-shadow: 0 12px 40px rgba(0, 0, 0, 0.08);
    border-color: var(--primary-border);
  }

  @media (prefers-color-scheme: dark) {
    .feat-card:hover {
      box-shadow: 0 12px 40px rgba(0, 0, 0, 0.3);
    }
  }

  .feat-card-lg {
    grid-column: span 1;
  }

  .feat-icon-wrap {
    width: 44px;
    height: 44px;
    border-radius: 12px;
    background: var(--primary-bg);
    display: flex;
    align-items: center;
    justify-content: center;
    margin-bottom: 1rem;
    font-size: 1.3rem;
  }

  .feat-name {
    font-size: 0.95rem;
    font-weight: 600;
    color: var(--text);
    margin-bottom: 0.5rem;
  }

  .feat-desc {
    font-size: 0.82rem;
    color: var(--text-muted);
    line-height: 1.6;
    margin-bottom: 0.75rem;
  }

  .feat-tags {
    display: flex;
    flex-wrap: wrap;
    gap: 0.4rem;
  }

  .feat-tag {
    font-family: 'DM Mono', monospace;
    font-size: 0.65rem;
    color: var(--primary-light);
    background: var(--primary-bg);
    border: 1px solid var(--primary-border);
    padding: 2px 8px;
    border-radius: 4px;
  }

  /* ─────────────────── HOW IT WORKS ─────────────────── */

  .how-it-works {
    padding: 6rem 0;
    background: var(--bg);
  }

  .steps {
    display: flex;
    align-items: flex-start;
    justify-content: center;
    gap: 0;
    max-width: 900px;
    margin: 0 auto;
  }

  .step {
    flex: 1;
    text-align: center;
    padding: 0 1.5rem;
  }

  .step-num {
    width: 48px;
    height: 48px;
    border-radius: 50%;
    background: var(--primary);
    color: white;
    font-family: 'Anton', sans-serif;
    font-size: 1.3rem;
    display: flex;
    align-items: center;
    justify-content: center;
    margin: 0 auto 1.25rem;
    box-shadow: 0 4px 16px rgba(37, 99, 235, 0.3);
  }

  .step-title {
    font-size: 1rem;
    font-weight: 600;
    color: var(--text);
    margin-bottom: 0.5rem;
  }

  .step-desc {
    font-size: 0.82rem;
    color: var(--text-muted);
    line-height: 1.6;
  }

  .step-connector {
    display: flex;
    align-items: center;
    padding-top: 24px;
  }

  .step-connector span {
    display: block;
    width: 40px;
    height: 2px;
    background: var(--border);
    border-radius: 2px;
  }

  /* ─────────────────── OPEN SOURCE ─────────────────── */

  .opensource {
    padding: 6rem 0;
    background: var(--bg-alt);
  }

  .os-card {
    display: flex;
    align-items: center;
    gap: 3rem;
    background: var(--bg-card);
    border: 1px solid var(--border);
    border-radius: 24px;
    padding: 3rem;
    overflow: hidden;
  }

  .os-content {
    flex: 1;
    min-width: 0;
  }

  .os-title {
    font-family: 'Anton', sans-serif;
    font-size: clamp(1.8rem, 3.5vw, 2.5rem);
    letter-spacing: 0.02em;
    color: var(--text);
    line-height: 1.1;
    margin: 0.75rem 0 1rem;
  }

  .os-desc {
    font-size: 0.9rem;
    color: var(--text-muted);
    line-height: 1.7;
    margin-bottom: 1.5rem;
  }

  .os-stack {
    display: flex;
    flex-wrap: wrap;
    gap: 0.6rem;
    margin-bottom: 1.75rem;
  }

  .stack-item {
    display: flex;
    align-items: center;
    gap: 0.4rem;
    font-size: 0.75rem;
    font-weight: 500;
    color: var(--text-muted);
    background: var(--primary-bg);
    border: 1px solid var(--primary-border);
    padding: 0.35rem 0.75rem;
    border-radius: 8px;
  }

  .stack-icon {
    display: flex;
    align-items: center;
    color: var(--primary-light);
    font-family: 'DM Mono', monospace;
    font-weight: 600;
    font-size: 0.7rem;
  }

  .os-btn {
    display: inline-flex;
    align-items: center;
    gap: 0.6rem;
    padding: 0.75rem 1.25rem;
    background: var(--primary);
    color: white;
    font-weight: 600;
    font-size: 0.88rem;
    text-decoration: none;
    border-radius: 10px;
    transition: background 0.15s, transform 0.1s, box-shadow 0.15s;
    box-shadow: 0 3px 12px rgba(37, 99, 235, 0.3);
  }

  .os-btn:hover {
    background: #1d4ed8;
    transform: translateY(-2px);
    box-shadow: 0 5px 20px rgba(37, 99, 235, 0.45);
  }

  .os-btn-arrow { transition: transform 0.2s; }
  .os-btn:hover .os-btn-arrow { transform: translateX(4px); }

  /* Code window */
  .os-visual {
    flex: 0 0 380px;
  }

  .code-window {
    border-radius: 12px;
    overflow: hidden;
    border: 1px solid var(--border);
    background: var(--bg);
    box-shadow: 0 8px 32px rgba(0, 0, 0, 0.06);
  }

  @media (prefers-color-scheme: dark) {
    .code-window {
      background: #0a0e1a;
      box-shadow: 0 8px 32px rgba(0, 0, 0, 0.3);
    }
  }

  .code-bar {
    display: flex;
    align-items: center;
    gap: 6px;
    padding: 0.75rem 1rem;
    background: var(--bg-alt);
    border-bottom: 1px solid var(--border);
  }

  @media (prefers-color-scheme: dark) {
    .code-bar { background: #0d1117; }
  }

  .dot {
    width: 10px;
    height: 10px;
    border-radius: 50%;
  }

  .dot.red { background: #ef4444; }
  .dot.yellow { background: #eab308; }
  .dot.green { background: #22c55e; }

  .code-title {
    margin-left: 0.5rem;
    font-family: 'DM Mono', monospace;
    font-size: 0.7rem;
    color: var(--text-faint);
  }

  .code-body {
    padding: 1.25rem 1rem;
    font-family: 'DM Mono', monospace;
    font-size: 0.72rem;
    line-height: 1.8;
    overflow-x: auto;
    color: var(--text-muted);
  }

  .c-comment { color: #64748b; }
  .c-keyword { color: #c084fc; }
  .c-var { color: #60a5fa; }
  .c-type { color: #34d399; }
  .c-prop { color: #f472b6; }
  .c-string { color: #fbbf24; }
  .c-bool { color: #f87171; }
  .c-func { color: #60a5fa; }

  /* ─────────────────── FINAL CTA ─────────────────── */

  .final-cta {
    padding: 6rem 0;
    background: var(--bg);
    text-align: center;
  }

  .cta-content {
    max-width: 550px;
    margin: 0 auto;
  }

  .cta-title {
    font-family: 'Anton', sans-serif;
    font-size: clamp(2rem, 4.5vw, 3.2rem);
    letter-spacing: 0.02em;
    color: var(--text);
    line-height: 1.1;
    margin-bottom: 1rem;
  }

  .cta-desc {
    font-size: 1rem;
    color: var(--text-muted);
    line-height: 1.6;
    margin-bottom: 2rem;
  }

  .cta-big {
    font-size: 1.05rem;
    padding: 1rem 1.75rem;
    display: inline-flex;
  }

  /* ─────────────────── FOOTER ─────────────────── */

  .footer {
    padding: 2rem 0;
    background: var(--bg-alt);
    border-top: 1px solid var(--border);
  }

  .footer-inner {
    display: flex;
    align-items: center;
    justify-content: space-between;
    margin-bottom: 1rem;
  }

  .footer-left {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    font-size: 0.82rem;
  }

  .footer-brand {
    font-family: 'Anton', sans-serif;
    font-size: 1rem;
    color: var(--text);
    letter-spacing: 0.02em;
  }

  .footer-sep { color: var(--text-faint); }

  .footer-copy {
    color: var(--text-muted);
    font-size: 0.78rem;
  }

  .footer-links {
    display: flex;
    gap: 1.25rem;
  }

  .footer-links a {
    font-size: 0.78rem;
    color: var(--text-muted);
    text-decoration: none;
    transition: color 0.15s;
  }

  .footer-links a:hover { color: var(--primary-light); }

  .footer-powered {
    text-align: center;
    font-family: 'DM Mono', monospace;
    font-size: 0.62rem;
    color: var(--text-faint);
    letter-spacing: 0.05em;
  }

  /* ─────────────────── RESPONSIVE ─────────────────── */

  @media (max-width: 900px) {
    .feat-grid {
      grid-template-columns: repeat(2, 1fr);
    }

    .feat-card-lg {
      grid-column: span 2;
    }

    .os-card {
      flex-direction: column;
      padding: 2rem;
    }

    .os-visual {
      flex: none;
      width: 100%;
      max-width: 420px;
    }

    .steps {
      flex-direction: column;
      gap: 1.5rem;
      align-items: center;
    }

    .step-connector {
      padding-top: 0;
      transform: rotate(90deg);
    }
  }

  @media (max-width: 600px) {
    .feat-grid {
      grid-template-columns: 1fr;
    }

    .feat-card-lg {
      grid-column: span 1;
    }

    .os-card {
      padding: 1.5rem;
    }

    .hero-ctas {
      flex-direction: column;
      align-items: center;
    }

    .hero-stats {
      gap: 1rem;
    }

    .stat-num { font-size: 1.2rem; }

    .footer-inner {
      flex-direction: column;
      gap: 1rem;
      text-align: center;
    }

    .nav-link span { display: none; }
    .gh-link { padding: 0.4rem; }

    .scroll-hint { display: none; }
  }

  @media (max-width: 400px) {
    .bot-name { font-size: 3rem; }
    .hero-desc { font-size: 0.92rem; }
  }
</style>

<!-- ═══════════════════════ SCRIPTS ═══════════════════════ -->
<script>
  // Navbar scroll effect
  const navbar = document.getElementById('navbar');
  const onScroll = () => {
    if (window.scrollY > 50) {
      navbar?.classList.add('scrolled');
    } else {
      navbar?.classList.remove('scrolled');
    }
  };
  window.addEventListener('scroll', onScroll, { passive: true });
  onScroll();

  // Intersection Observer for scroll animations
  const observerOptions = { threshold: 0.1, rootMargin: '0px 0px -50px 0px' };
  const observer = new IntersectionObserver((entries) => {
    entries.forEach(entry => {
      if (entry.isIntersecting) {
        entry.target.classList.add('visible');
        observer.unobserve(entry.target);
      }
    });
  }, observerOptions);

  document.querySelectorAll('.feat-card, .step, .os-card').forEach(el => {
    el.classList.add('animate-on-scroll');
    observer.observe(el);
  });
</script>

<style>
  .animate-on-scroll {
    opacity: 0;
    transform: translateY(20px);
    transition: opacity 0.6s ease, transform 0.6s ease;
  }

  .animate-on-scroll.visible {
    opacity: 1;
    transform: translateY(0);
  }

  .feat-card.animate-on-scroll:nth-child(2) { transition-delay: 0.05s; }
  .feat-card.animate-on-scroll:nth-child(3) { transition-delay: 0.1s; }
  .feat-card.animate-on-scroll:nth-child(4) { transition-delay: 0.15s; }
  .feat-card.animate-on-scroll:nth-child(5) { transition-delay: 0.2s; }
  .feat-card.animate-on-scroll:nth-child(6) { transition-delay: 0.25s; }
  .feat-card.animate-on-scroll:nth-child(7) { transition-delay: 0.3s; }
  .feat-card.animate-on-scroll:nth-child(8) { transition-delay: 0.35s; }
  .feat-card.animate-on-scroll:nth-child(9) { transition-delay: 0.4s; }
  .feat-card.animate-on-scroll:nth-child(10) { transition-delay: 0.45s; }
</style>

</body>
</html>
